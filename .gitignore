#IDE (GoLand) specific 
.idea/

# Temporary binaries for bulding the cluster (i.e: kubectl)
contrib/bin

ovn-kubernetes-anp-test-report.yaml

**/ginkgo.report

# Claude Code worktrees (created per-session, not part of repo history)
.claude/worktrees/
